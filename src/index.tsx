import * as React from "react";
import * as ReactDOM from "react-dom";
import PasswordStrength from "./components/PasswordStrength";

ReactDOM.render(<PasswordStrength />, document.getElementById("root"));
